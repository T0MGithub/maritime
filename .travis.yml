language: node_js
node_js:
  - "9.11"
  - "10.15"
  - "11.15"
  - "12.3"
cache:
  directories:
    - node_modules
script:
  - npm run lint
  - npm run coverage
after_script:
  # upload coverage to coveralls
  npm install --save-dev coveralls
  coveralls < ./coverage/lcov.info
